# OS files
.DS_Store
Thumbs.db

# Build output
SaatSaheli/target/
SSMainApp/target/
FrontEnd/node_modules/
FrontEnd/node/
FrontEnd/build/

# IDE
.idea/
*.iml
.vscode/
*.swp

# Credentials - never commit these
*.json
!FrontEnd/public/manifest.json
!FrontEnd/package.json
!FrontEnd/package-lock.json

# Uploads
SaatSaheli/uploads/

# Environment — never commit .env (secrets), but .env.example is safe
.env
.env.local
.env.*.local
!.env.example
!FrontEnd/.env.example

# Local dev seed scripts (hardcoded IDs, not for shared use)
populate_magazine*.py

# Neon CLI local state
.neon

# iCloud-renamed node_modules (workaround, not real source)
FrontEnd/node_modules.nosync/

# One-off local assets not part of the app
saatsaheli-logo.png
saatsaheli-stickers.html
